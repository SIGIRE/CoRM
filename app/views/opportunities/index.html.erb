<%
  filter = 'filter'
  index = 'index'
  if @account
    filter += '_when_account'
    index += '_when_account'
  end
%>

<%= render 'title', :locals => { :title => @title }%>
<%= render partial: filter %>
<%= render partial: index %>
