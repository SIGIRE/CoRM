<%= render 'title', :locals => { :title => @title }%>

<% if @emails.length >= 1 %>
	<%= render 'emails' %>
<% end %>

<%# puts "LONGUEUR : #{@tasks.length}" %>
<%# if @tasks.length >= 1 %>
	<%#= render 'tasks' %>
<%# end %>
